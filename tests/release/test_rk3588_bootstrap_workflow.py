from pathlib import Path   ROOT = Path(__file__).resolve().parents[2] WORKFLOW = ROOT / ".github" / "workflows" / "rk3588-service-bootstrap.yml" CONTROL = ROOT / "deploy" / "control.sh"   def test_rk3588_service_bootstrap_workflow_installs_deploys_and_verifies_runtime():     assert WORKFLOW.is_file(), "RK3588 service bootstrap workflow is missing"     workflow = WORKFLOW.read_text(encoding="utf-8")      assert "name: RK3588 Service Bootstrap" in workflow     assert "workflow_dispatch:" in workflow     assert "runs-on: [self-hosted, rk3588]" in workflow     assert "cancel-in-progress: true" in workflow     assert "SUSPECT_DEPLOY_PORT: '18080'" in workflow     assert "sudo -n true" in workflow     assert "codeload.github.com/${GITHUB_REPOSITORY}/tar.gz/${GITHUB_SHA}" in workflow     assert 'SOURCE_ROOT="$(find "$SOURCE_STAGE" -mindepth 1 -maxdepth 1 -type d -print -quit)"' in workflow     assert 'test -f "$SOURCE_ROOT/deploy/control.sh"' in workflow     assert "git sparse-checkout init --cone" not in workflow     assert 'git -c protocol.version=2 fetch' not in workflow     assert 'TEST_RUNTIME_ROOT="$RUNNER_TEMP/suspect-bootstrap-test"' in workflow     assert 'export SUSPECT_DB_PATH="$TEST_RUNTIME_ROOT/interrogation.db"' in workflow     assert 'export SUSPECT_DATA_DIR="$TEST_RUNTIME_ROOT/data"' in workflow     assert 'export SUSPECT_LOG_DIR="$TEST_RUNTIME_ROOT/log"' in workflow     assert "sudo -n systemctl stop interrogation-api.service" not in workflow     assert "existing project API on TCP/18080 is preserved during speech preflight" in workflow     assert "Existing TCP/8000 owner is preserved" in workflow     assert "sudo -n ss -ltnp 'sport = :8000'" in workflow     assert "sudo -n ss -ltnp \"sport = :${SUSPECT_DEPLOY_PORT}\"" in workflow     assert "SUSPECT_API_PORT=${SUSPECT_DEPLOY_PORT}" in workflow     assert "SUSPECT_KIOSK_READY_URL=https://127.0.0.1:${SUSPECT_DEPLOY_PORT}/health/ready" in workflow     assert "SUSPECT_KIOSK_URL=https://127.0.0.1:${SUSPECT_DEPLOY_PORT}/" in workflow     assert "sudo -n bash deploy/control.sh install" in workflow     assert "SUSPECT_HEALTH_BASE_URL=\"https://127.0.0.1:${SUSPECT_DEPLOY_PORT}\"" in workflow     assert "systemctl is-enabled --quiet interrogation-api.service" in workflow     assert "systemctl is-active --quiet interrogation-api.service" in workflow     assert "systemctl is-enabled --quiet kiosk.service" in workflow     assert "curl --cacert /etc/suspect-interrogation/tls/ca.crt -fsS \"https://127.0.0.1:${SUSPECT_DEPLOY_PORT}/health/ready\"" in workflow     assert "curl --cacert /etc/suspect-interrogation/tls/ca.crt -fsS \"https://127.0.0.1:${SUSPECT_DEPLOY_PORT}/\"" in workflow   def test_bootstrap_prepares_read_only_funasr_model_and_self_healing_runtime_layout():     workflow = WORKFLOW.read_text(encoding="utf-8")     assert "FUNASR_SOURCE=/home/youyeetoo/funasr-models" in workflow     assert "FUNASR_MODEL_ROOT=/opt/suspect-interrogation/models/funasr" in workflow     assert "FUNASR_RUNTIME=/opt/suspect-interrogation/runtime/funasr-env" in workflow     assert 'sudo -n mount --bind "$FUNASR_SOURCE" "$FUNASR_MODEL_ROOT"' in workflow     assert 'sudo -n mount -o remount,bind,ro "$FUNASR_MODEL_ROOT"' in workflow     assert 'findmnt -no OPTIONS "$FUNASR_MODEL_ROOT"' in workflow     assert 'test -d "$FUNASR_MODEL_ROOT/paraformer"' in workflow     assert 'test -d "$FUNASR_MODEL_ROOT/fsmn-vad"' in workflow     assert 'test -d "$FUNASR_MODEL_ROOT/eres2net-large"' in workflow     assert 'test -s "$FUNASR_MODEL_ROOT/eres2net-large/pretrained_eres2net.pt"' in workflow     assert "/etc/fstab" in workflow     assert "x-systemd.before=ai-worker.service" in workflow     assert "runtime_core_ok=0" in workflow     assert 'if [[ -x "$FUNASR_RUNTIME/bin/python" ]]' in workflow     assert 'if [[ "$runtime_core_ok" -ne 1 ]]' in workflow     assert 'sudo -n python3 -m venv "$FUNASR_RUNTIME"' in workflow     assert "https://download.pytorch.org/whl/cpu" in workflow     assert '"torch==${TORCH_CPU_VERSION}+cpu"' in workflow     assert '"funasr==${FUNASR_PACKAGE_VERSION}"' in workflow     assert '"torchaudio==${TORCHAUDIO_PACKAGE_VERSION}"' in workflow     assert "MODELSCOPE_PACKAGE_VERSION" not in workflow     assert '"modelscope==${MODELSCOPE_PACKAGE_VERSION}"' not in workflow     assert 'MODEL_ROOT="$FUNASR_MODEL_ROOT"' in workflow     assert "scripts/ci/probe-funasr-runtime.py" in workflow     assert 'sudo -n chown -R root:root "$FUNASR_RUNTIME"' in workflow   def test_install_upserts_funasr_runtime_env_for_existing_installations():     control = CONTROL.read_text(encoding="utf-8")      assert 'upsert_runtime_env "SUSPECT_FUNASR_MODEL_ROOT" "/opt/suspect-interrogation/models/funasr"' in control     assert 'upsert_runtime_env "SUSPECT_FUNASR_PYTHON" "/opt/suspect-interrogation/runtime/funasr-env/bin/python"' in control     assert 'upsert_runtime_env "SUSPECT_ERES2NET_MODEL_DIR" "/opt/suspect-interrogation/models/funasr/eres2net-large"' in control     assert 'upsert_runtime_env "SUSPECT_SPEAKER_BACKEND" "eres2net_large"' in control     assert 'remove_runtime_env "SUSPECT_XVECTOR_LEGACY_PYTHON"' in control     assert 'upsert_runtime_env "SUSPECT_SPEECH_SOCKET" "/run/suspect-interrogation/speech.sock"' in control     assert 'upsert_runtime_env "AI_MODE" "real"' in control     assert 'upsert_runtime_env "ALSA_DEVICE" "plughw:1,0"' in control     assert "ALC Capture Function" in control     assert 'grep -q "^${key}=" "$runtime_env"' in control     assert 'sed -i -E "s|^${key}=.*|${key}=${value}|" "$runtime_env"' in control
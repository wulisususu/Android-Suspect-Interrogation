# 本地业务后端（仅浏览器业务联调）
HOST=127.0.0.1
PORT=8080
DB_PATH=./data/suspect-interrogation.db

# 默认关闭设备模拟。真实硬件未接入时返回 DEVICE_NOT_CONNECTED。
DEVICE_SIMULATOR=0

# AI 不在 backend-dev 中运行。
# 正式 AI 推理仅通过 Android APK 内的本地 LLM Runtime 执行。

# CI Trigger

The Linux release branch is intentionally validated through a pull request targeting `linux-adaptation` so the `pull_request` event runs the full hosted gate before the RK3588 smoke job.

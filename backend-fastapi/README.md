# FastAPI Backend

Linux adaptation backend migration layer.

## Current

- FastAPI service
- SQLAlchemy models
- API compatibility layer foundation
- AI interfaces reserved

## Run

```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 18000
```

Future:

FastAPI -> LangGraph -> RKLLM/RKNN

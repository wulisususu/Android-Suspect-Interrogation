# 现阶段保持旧系统远端 API，不提前迁移后端。
VITE_API_BASE_URL=https://uat.pediatrician-ai.fb.jnpinno.com/

# 可留空；运行时也会尝试读取 localStorage.user_token。
VITE_AUTH_TOKEN=

# 保持旧链路兼容；如果后续 token 需要 Bearer 前缀，可改为 auto。
VITE_AUTH_MODE=raw

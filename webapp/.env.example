# 留空时使用当前同源地址；连接独立 Linux 后端时可填写完整地址。
VITE_API_BASE_URL=

# 正式默认 linux-http-ws；仅浏览器开发联调时可设为 browser-dev。
VITE_RUNTIME_MODE=linux-http-ws

# 正式一体机必须保持 ALSA。局域网 Windows 浏览器麦克风测试时设为 BROWSER，
# 或使用 URL 查询参数 ?audioInput=browser 临时覆盖。
VITE_AUDIO_INPUT_MODE=ALSA

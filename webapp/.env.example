# 默认留空，浏览器开发时使用 Vite 同源代理。只有连接独立联调后端时才填写完整地址。
# 正式 Android APK 内业务调用通过 window.NativeBridge -> Kotlin，完全不依赖此配置。
VITE_API_BASE_URL=

# 临时云端 AI 只允许 backend-dev 联调使用；正式 APK 不写死智谱/DeepSeek/UAT 地址。
VITE_AUTH_TOKEN=
VITE_AUTH_MODE=raw

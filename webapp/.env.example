# 仅浏览器开发模式使用。正式 Android APK 内业务调用通过 window.NativeBridge -> Kotlin，完全不依赖 8080。
VITE_API_BASE_URL=http://127.0.0.1:8080/

# 临时云端 AI 只允许 backend-dev 联调使用；正式 APK 不写死智谱/DeepSeek/UAT 地址。
VITE_AUTH_TOKEN=
VITE_AUTH_MODE=raw

# 本地业务后端
HOST=127.0.0.1
PORT=8080
DB_PATH=./data/suspect-interrogation.db

# 保留现有 AI/DeepSeek 上游，不在本后端替换模型能力。
# 当前项目旧链路默认值：
AI_UPSTREAM_BASE_URL=https://uat.pediatrician-ai.fb.jnpinno.com/
AI_UPSTREAM_TIMEOUT_MS=120000

# 默认关闭设备模拟。真实硬件未接入时返回 DEVICE_NOT_CONNECTED。
DEVICE_SIMULATOR=0
